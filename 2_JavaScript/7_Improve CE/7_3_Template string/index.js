const recipient = "james"

const sender = "Roy"

const email = `Hey ${recipient}! How is it going? ${sender}`

console.log(email)

//Break the email string into multiplre string

const emails = `
Hey ${recipient}!

How is it going? 
    
${sender} 
`

console.log(emails)